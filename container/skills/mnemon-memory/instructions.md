# Mnemon Memory

Usa `mnemon` como memoria de grafo duradera. Antes de trabajo que dependa del
contexto previo, ejecuta `mnemon recall "<tarea o tema>"`. Despues de decisiones
importantes, preferencias o flujos repetibles, ejecuta
`mnemon remember "<nota duradera>"`. Nunca guardes secretos.
